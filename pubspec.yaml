name: text_to_speech_platform_interface
description: A common platform interface for text_to_speech plugin
version: 0.1.0
repository: https://github.com/ixsans/text_to_speech_platform_interface

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter

